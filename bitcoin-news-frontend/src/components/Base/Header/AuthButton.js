import React from 'react';

const AuthButton = () => {
    return(
        <div className="auth-button-wrapper">
            <div className="auth-button">로그인/회원가입</div>
        </div>
    );
};

export default AuthButton;
